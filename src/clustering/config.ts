export const NOISE = -1,
    UNCLASSIFIED = -2 /**
 * Threshold for considering two distances as equal.
 * This is used to avoid floating-point precision issues when comparing distances.
 * A value of 1e-12 is typically sufficient for most applications.
 */
export const EPS = 1e-12
